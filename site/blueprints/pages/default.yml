title: About

icon: 👤

options:
  # Allow publishing / unpublishing (draft → listed) so new or duplicated pages
  # can be made live from the Panel.
  changeStatus: true

tabs:
  content:
    label: Contenu
    icon: text
    columns:
      main:
        width: 1/1
        fields:
          content_secondary:
            label: Contenu supplémentaire (à gauche sur desktop)
            type: blocks
            fieldsets:
              - bullet_points
              - image
              - text
              - video

          content:
            label: Contenu
            type: blocks
            fieldsets:
              - bullet_points
              - image
              - text
              - video

  seo:
    extends: tabs/seo
